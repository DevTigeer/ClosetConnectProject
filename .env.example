# ===================================
# ClosetConnect 환경변수 설정 예시
# ===================================
# 이 파일을 .env로 복사하고 실제 값으로 변경하세요
# cp .env.example .env
#
# 주의: .env 파일은 절대 Git에 커밋하지 마세요!
# (.gitignore에 .env가 포함되어 있는지 확인)

# ===================================
# DATABASE 설정
# ===================================
DB_URL=jdbc:mariadb://localhost:3306/closetConnectProject
DB_USERNAME=root
DB_PASSWORD=your_database_password_here

# ===================================
# JWT 설정
# ===================================
# JWT 시크릿 키 (최소 256비트 = 43자 이상 권장)
# 생성 방법: openssl rand -base64 64
JWT_SECRET=your-very-long-random-secret-key-at-least-256-bits-change-this-in-production
JWT_TOKEN_VALIDITY=3600

# ===================================
# TOSS PAYMENTS 설정
# ===================================
# 토스페이먼츠 API 키 발급: https://developers.tosspayments.com/
# 개발: test_로 시작, 운영: live_로 시작

# 클라이언트 키 (공개키 - 브라우저에서 사용)
TOSS_CLIENT_KEY=test_gck_your_client_key_here

# 시크릿 키 (비밀키 - 서버에서만 사용, 절대 노출 금지!)
TOSS_SECRET_KEY=test_gsk_your_secret_key_here

# ===================================
# GOOGLE AI STUDIO 설정
# ===================================
# Google AI Studio API 키 (Gemini 2.5 Flash Image - Nano Banana)
# 발급 방법: https://aistudio.google.com/apikey
# 무료: 하루 1,500 요청 가능
GOOGLE_API_KEY=your_google_api_key_here

# Nano Banana 이미지 확장 프롬프트 (선택 사항)
# 비워두면 기본 프롬프트 사용
# 예시: "Extend the clothing naturally with seamless edges, maintaining original colors and patterns"
IMAGEN_EXPAND_PROMPT=옷을 입은 이미지에서 옷만 추출한거야 해당 옷을 잘리지않게 생성해줘.

# Nano Banana API 호출 간 지연 시간 (초)
# 무료 티어 Rate limit 방지 (권장: 2-5초, 기본값: 2초)
IMAGEN_API_DELAY=3

# ===================================
# 기타 설정
# ===================================
# REMBG 서버 URL (배경 제거 서비스)
REMBG_SERVER_URL=http://localhost:8001

# 업로드 디렉토리
UPLOAD_DIR=./uploads
