# 👔 날씨 기반 옷 추천 시스템

## 개요

온도, 날씨 상태, 습도를 기반으로 적절한 옷차림을 자동 추천하는 기능입니다.

---

## ✨ 주요 기능

### 1. **체감온도 자동 계산**
- **맑음**: 햇빛 효과로 +1°C 보정
- **습도 70% 이상**: 불쾌지수 반영 +2°C 보정
- **비/눈**: 체감온도는 유지하되, 우산/방수 재킷 등 추가 아이템 추천

### 2. **온도 구간별 자동 추천**

| 체감온도 | 추천 상의 | 추천 하의 | 추가 아이템 |
|---------|---------|---------|------------|
| 28°C 이상 | 민소매, 반팔티 | 반바지, 짧은 치마 | 선글라스, 모자 |
| 23-27°C | 반팔티, 얇은 셔츠 | 반바지, 면바지 | 얇은 가디건 |
| 20-22°C | 긴팔티, 얇은 니트 | 면바지, 청바지 | 얇은 가디건 |
| 17-19°C | 니트, 맨투맨 | 청바지, 면바지 | 얇은 재킷 |
| 12-16°C | 니트, 후드티 | 청바지 | 자켓, 야상 |
| 9-11°C | 니트, 스웨터 | 기모 바지 | 트렌치코트 |
| 5-8°C | 니트, 기모 후드티 | 기모 바지 | 코트, 머플러 |
| 4°C 이하 | 두꺼운 니트, 내복 | 기모 바지 | 패딩, 목도리, 장갑 |

### 3. **날씨 상태별 보정**
- **비/소나기**: 우산, 우비, 방수 재킷 추가
- **눈**: 방한용품, 미끄럼 방지 신발 추가
- **천둥번개**: 외출 주의 안내
- **습도 높음**: 통풍 잘되는 소재 추천

---

## 🌐 API 엔드포인트

### 1. 도시 코드로 옷 추천 조회

```http
GET /api/v1/weather/recommendation/{cityCode}
```

**파라미터:**
- `cityCode`: 도시 코드 (seoul, incheon, busan, daegu, gwangju, jeju)

**예시:**
```http
GET /api/v1/weather/recommendation/seoul
```

**응답 예시:**
```json
{
  "currentTemperature": 15.0,
  "feelsLikeTemperature": 16.0,
  "weatherCondition": "맑음",
  "humidity": 50,
  "topRecommendation": "얇은 니트, 맨투맨, 후드티, 긴팔티",
  "bottomRecommendation": "청바지, 면바지, 슬랙스",
  "extraRecommendation": "얇은 재킷, 가디건, 바람막이",
  "recommendationMessage": "오늘은 15.0°C 로 맑음 날씨예요.\n체감 온도는 약 16.0°C 정도예요.\n\n추천 코디는 다음과 같아요:\n- 상의: 얇은 니트, 맨투맨, 후드티, 긴팔티\n- 하의: 청바지, 면바지, 슬랙스\n- 추가: 얇은 재킷, 가디건, 바람막이\n\n오늘 날씨에 잘 맞는 스타일로 하루를 보내세요!"
}
```

---

### 2. 위도/경도로 옷 추천 조회

```http
GET /api/v1/weather/recommendation?latitude={lat}&longitude={lon}
```

**파라미터:**
- `latitude`: 위도 (필수)
- `longitude`: 경도 (필수)

**예시:**
```http
GET /api/v1/weather/recommendation?latitude=37.57&longitude=126.98
```

**응답:** 위와 동일

---

### 3. 수동 입력값으로 테스트

```http
GET /api/v1/weather/recommendation/custom?temperature={temp}&weatherCondition={condition}&humidity={humidity}
```

**파라미터:**
- `temperature`: 온도 (필수)
- `weatherCondition`: 날씨 상태 (선택, 기본값: "맑음")
- `humidity`: 습도 (선택, 기본값: 50)

**예시:**
```http
GET /api/v1/weather/recommendation/custom?temperature=25&weatherCondition=비&humidity=80
```

**응답:**
```json
{
  "currentTemperature": 25.0,
  "feelsLikeTemperature": 27.0,
  "weatherCondition": "비",
  "humidity": 80,
  "topRecommendation": "반팔티, 얇은 셔츠, 블라우스",
  "bottomRecommendation": "반바지, 면바지, 치마",
  "extraRecommendation": "얇은 가디건, 모자, 선글라스, 우산, 우비/방수 재킷, 방수 신발, 통풍 잘되는 소재 추천",
  "recommendationMessage": "오늘은 25.0°C 로 비 날씨예요.\n체감 온도는 약 27.0°C 정도예요.\n\n추천 코디는 다음과 같아요:\n- 상의: 반팔티, 얇은 셔츠, 블라우스\n- 하의: 반바지, 면바지, 치마\n- 추가: 얇은 가디건, 모자, 선글라스, 우산, 우비/방수 재킷, 방수 신발, 통풍 잘되는 소재 추천\n\n오늘 날씨에 잘 맞는 스타일로 하루를 보내세요!"
}
```

---

## 🎨 프론트엔드 UI

### 접속 방법

```
http://localhost:8080/weather-with-recommendation.html
```

### 화면 구성

1. **위치 선택**
   - 한국 6개 주요 도시 버튼
   - "현재 위치 사용" 버튼

2. **현재 날씨 카드**
   - 현재 기온 + 날씨 상태
   - 오늘 최고/최저 기온

3. **옷 추천 카드** (핑크 그라디언트)
   - 체감 온도 표시
   - 추천 상의/하의/추가 아이템
   - 최종 추천 문장

---

## 📊 체감온도 계산 로직

### 공식
```java
체감온도 = 실제온도 + 날씨보정 + 습도보정

날씨보정:
  - 맑음: +1°C
  - 기타: 0°C

습도보정:
  - 70% 이상: +2°C
  - 70% 미만: 0°C
```

### 예시

**케이스 1: 맑고 습도 낮음**
```
실제온도: 15°C
날씨: 맑음 (+1°C)
습도: 50% (0°C)
→ 체감온도: 16°C
→ 구간: 12-16°C (니트, 후드티 추천)
```

**케이스 2: 흐리고 습도 높음**
```
실제온도: 25°C
날씨: 흐림 (0°C)
습도: 80% (+2°C)
→ 체감온도: 27°C
→ 구간: 23-27°C (반팔티, 반바지 추천 + 통풍 소재)
```

**케이스 3: 비오고 습도 높음**
```
실제온도: 18°C
날씨: 비 (0°C, 하지만 우산 추가)
습도: 75% (+2°C)
→ 체감온도: 20°C
→ 구간: 20-22°C (긴팔티 + 우산/우비)
```

---

## 💻 사용 예시

### Java (Service 호출)

```java
@Autowired
private ClothingRecommendationService recommendationService;

// 옷 추천 받기
ClothingRecommendation rec = recommendationService.getRecommendation(
    15.0,     // 온도
    "맑음",   // 날씨 상태
    50        // 습도
);

System.out.println("체감온도: " + rec.getFeelsLikeTemperature() + "°C");
System.out.println("추천 상의: " + rec.getTopRecommendation());
System.out.println("\n" + rec.getRecommendationMessage());
```

### JavaScript (프론트엔드)

```javascript
// 서울 옷 추천 조회
const response = await fetch('/api/v1/weather/recommendation/seoul');
const data = await response.json();

console.log('체감온도:', data.feelsLikeTemperature);
console.log('추천 상의:', data.topRecommendation);
console.log('추천 메시지:', data.recommendationMessage);

// 커스텀 값으로 테스트
const custom = await fetch('/api/v1/weather/recommendation/custom?temperature=10&weatherCondition=눈&humidity=60');
const customData = await custom.json();
```

---

## 🔧 커스터마이징

### 온도 구간 수정

`TemperatureRange.java` 파일에서 구간 수정 가능:

```java
COOL(12, 16,
    "니트, 맨투맨, 후드티, 긴팔 셔츠",  // 상의
    "청바지, 면바지, 슬랙스",          // 하의
    "자켓, 가디건, 야상, 청자켓"),     // 추가
```

### 보정 값 변경

`ClothingRecommendationService.java`의 `calculateFeelsLikeTemperature()` 메서드:

```java
if (condition.contains("맑음")) {
    adjustment += 1.0; // 여기를 2.0으로 변경하면 맑음 효과 강화
}

if (humidity >= 70) {
    adjustment += 2.0; // 습도 보정값 조정
}
```

---

## 🚀 실행 방법

1. **서버 시작**
   ```bash
   ./gradlew bootRun
   ```

2. **브라우저 접속**
   ```
   http://localhost:8080/weather-with-recommendation.html
   ```

3. **API 테스트**
   ```bash
   # 서울 옷 추천
   curl http://localhost:8080/api/v1/weather/recommendation/seoul

   # 커스텀 값 테스트
   curl "http://localhost:8080/api/v1/weather/recommendation/custom?temperature=5&weatherCondition=눈&humidity=70"
   ```

---

## 📂 파일 구조

```
Weather/
├── Controller/
│   └── WeatherController.java          (+3개 엔드포인트 추가)
├── Service/
│   ├── WeatherService.java
│   └── ClothingRecommendationService.java  ✅ 새로 추가
├── Dto/
│   ├── ClothingRecommendation.java         ✅ 새로 추가
│   ├── TemperatureRange.java               ✅ 새로 추가 (Enum)
│   ├── WeatherCode.java
│   └── WeatherResponse.java
└── Client/
    └── OpenMeteoResponse.java

static/
└── weather-with-recommendation.html        ✅ 새로 추가
```

---

## 💡 확장 아이디어

- [ ] 사용자 체형/선호도 반영 (키, 몸무게 입력 시 더 정확한 추천)
- [ ] AI 기반 스타일 추천 (머신러닝 모델 연동)
- [ ] 옷장(Closet) 연동 (내 옷장에 있는 아이템 중 추천)
- [ ] 날씨 알림 (특정 조건 만족 시 푸시 알림)
- [ ] 과거 날씨 + 옷차림 데이터 학습

---

## 📞 문의

옷 추천 로직 관련 문의는 프로젝트 이슈로 등록해주세요.
